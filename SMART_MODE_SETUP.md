# تفعيل "الوضع الذكي" (ربط التطبيق بـ Claude) — دليل خطوة بخطوة

هذا الدليل موجّه لمن لم يسبق له نشر أي خادم أو خدمة سحابية من قبل. لا يحتاج
تثبيت أي برنامج على جهازك — كل شيء عبر موقع Cloudflare مباشرة.

**تذكير:** التطبيق يعمل أوفلاين بالكامل بدون هذه الخطوة. "الوضع الذكي"
اختياري تمامًا لمن يريد إجابات حوارية حرة بدل مطابقة الكلمات الحرفية فقط.

---

## الخطوة 1 — احصل على مفتاح Anthropic API

1. اذهب إلى: https://console.anthropic.com
2. أنشئ حسابًا (أو سجّل الدخول إن كان عندك واحد).
3. من القائمة، اذهب إلى قسم **API Keys**، وأنشئ مفتاحًا جديدًا.
4. انسخ المفتاح واحفظه في مكان آمن (يبدأ عادة بـ `sk-ant-`) — لن يُعرض لك
   مرة أخرى بعد إغلاق الصفحة.

> ملاحظة: استخدام الـ API له تكلفة حسب حجم استخدامك، تُحاسَب من نفس الحساب.
> راجع صفحة التسعير في نفس الموقع لمعرفة الأرقام الحالية.

## الخطوة 2 — أنشئ حساب Cloudflare مجاني

1. اذهب إلى: https://dash.cloudflare.com/sign-up
2. أنشئ حسابًا ببريدك الإلكتروني (لا يحتاج بطاقة دفع للاستخدام الخفيف).

## الخطوة 3 — أنشئ الـ Worker (الخادم الوسيط)

1. من لوحة التحكم، افتح من القائمة الجانبية: **Workers & Pages**.
2. اضغط **Create** ثم **Create Worker**.
3. أعطه اسمًا مثل `quran-irab-ai` ثم اضغط **Deploy** (سينشئ نسخة تجريبية فارغة أولًا، هذا طبيعي).
4. بعد الإنشاء، اضغط **Edit code** (أو "Continue to project" ثم "Edit code").
5. احذف كل الكود الافتراضي الموجود في المحرر.
6. افتح ملف `worker.js` المرفق في هذا المشروع (داخل مجلد `cloudflare-worker/`)،
   وانسخ محتواه بالكامل، والصقه في المحرر.
7. اضغط **Save and Deploy** (أو **Deploy**).

## الخطوة 4 — أضف مفتاح API بأمان

1. من صفحة الـ Worker نفسه، اذهب إلى تبويب **Settings**.
2. ابحث عن قسم **Variables and Secrets** (أو "Environment Variables").
3. اضغط **Add** واختر النوع **Secret** (وليس Text — هذا مهم لإخفاء المفتاح).
4. الاسم: `ANTHROPIC_API_KEY`
   القيمة: الصق مفتاح API الذي نسخته في الخطوة 1.
5. احفظ (Save and deploy).

## الخطوة 5 — انسخ رابط الـ Worker

في أعلى صفحة الـ Worker ستجد رابطًا شكله تقريبًا:

```
https://quran-irab-ai.your-name.workers.dev
```

انسخه — هذا هو الرابط الذي سيستخدمه التطبيق.

## الخطوة 6 — فعّله داخل التطبيق

1. افتح تطبيق "روبوت إعراب القرآن" على جهازك.
2. في قسم "اسأل"، اضغط **⚙ إعداد رابط الخادم**.
3. الصق الرابط الذي نسخته، واضغط **حفظ**.
4. فعّل مفتاح **الوضع الذكي** بجانبه.
5. جرّب: افتح أي آية، واكتب سؤالًا حرًا مثل "ليش الكلمة هذي منصوبة؟" واضغط اسأل.

---

## استكشاف الأخطاء

| المشكلة | الحل |
|---|---|
| "فعّل الوضع الذكي أولًا واضبط رابط الخادم" | تأكد من لصق الرابط صحيحًا في الإعدادات وحفظه |
| "تعذّر الاتصال بالخادم" | تحقق من اتصال الإنترنت على جهازك |
| "خطأ من واجهة Claude API" | تأكد أن مفتاح `ANTHROPIC_API_KEY` مضبوط بشكل صحيح في Settings الخاصة بالـ Worker، وأن رصيدك في console.anthropic.com كافٍ |
| الرابط يعطي صفحة فارغة في المتصفح | هذا طبيعي — الـ Worker مصمم ليستقبل طلبات POST من التطبيق فقط، وليس للفتح المباشر في المتصفح |
